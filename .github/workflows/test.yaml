name: Run unit tests

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Run tests
        working-directory: stepik/test
        run: python3 -m unittest discover -s stepik/test
